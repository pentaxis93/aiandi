---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);

// Feature the most recent post
const [featuredPost, ...olderPosts] = posts;
---

<!doctype html>
<html lang="en" class="min-h-screen">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body class="min-h-screen flex flex-col bg-background text-text-primary font-body antialiased relative">
		<!-- Visible grid structure -->
		<div class="fixed inset-0 pointer-events-none" style="opacity: var(--theme-grid-opacity, 0.04); background-image: linear-gradient(var(--color-border) 1px, transparent 1px), linear-gradient(90deg, var(--color-border) 1px, transparent 1px); background-size: 32px 32px;"></div>
		
		<Header />
		
		<main class="flex-1 w-full relative z-10 px-6">
			<!-- Hero Section - Asymmetric, pushed left -->
			<section class="max-w-6xl mx-auto py-16 md:py-24 grid md:grid-cols-12 gap-8">
				<!-- Main content - left 8 columns -->
				<div class="md:col-span-8 border-l-4 border-accent pl-8">
					<div class="font-mono text-xs uppercase tracking-wider text-text-muted mb-6">
						Field Notes // Human-AI Collaboration
					</div>
					<h1 class="font-heading text-4xl md:text-6xl leading-[1.1] mb-8 tracking-[-0.02em]">
						ANCIENT PATTERNS<br />
						<span class="text-accent">IN NEW TOOLS</span>
					</h1>
					<p class="text-base md:text-lg leading-[1.8] text-text-secondary max-w-[58ch] mb-8">
						{SITE_DESCRIPTION}
					</p>
					<div class="font-mono text-xs text-text-muted border-t border-border pt-4 mt-8">
						CATALOG // ONGOING RESEARCH // EST. 2026
					</div>
				</div>
				
				<!-- Metadata sidebar - right 4 columns -->
				<aside class="md:col-span-4 border-2 border-border border-l-4 border-l-accent bg-surface p-6 font-mono text-xs space-y-4">
					<div>
						<div class="text-text-muted uppercase mb-1 tracking-wider">STATUS</div>
						<div class="text-accent font-bold">Active Research</div>
					</div>
					<div>
						<div class="text-text-muted uppercase mb-1 tracking-wider">DISCIPLINE</div>
						<div class="text-text-primary font-bold">Comp. Philosophy</div>
					</div>
					<div>
						<div class="text-text-muted uppercase mb-1 tracking-wider">METHOD</div>
						<div class="text-text-primary font-bold">Collaboration</div>
					</div>
					<div class="border-t border-border pt-4 mt-4">
						<div class="text-text-muted leading-relaxed">56, beginner, figuring it out in public</div>
					</div>
				</aside>
			</section>

			<!-- Featured Specimen -->
			{featuredPost && (
				<section class="max-w-6xl mx-auto mb-20">
					<div class="border-t-2 border-accent mb-1"></div>
					<div class="flex items-baseline gap-4 mb-8 font-mono text-xs uppercase tracking-wider">
						<span class="text-accent">SPECIMEN_001</span>
						<span class="text-text-muted flex-1">// LATEST ENTRY</span>
					</div>
					
					<article class="group border-2 border-border bg-surface hover:border-accent transition-colors duration-normal">
						<a 
							href={`/blog/${featuredPost.id}/`}
							class="block"
						>
							<!-- Specimen header -->
							<div class="border-b-2 border-border bg-surface-alt px-8 py-4 font-mono text-xs">
								<div class="flex items-baseline justify-between gap-4">
									<span class="text-text-muted uppercase tracking-wider">CATALOG NO.</span>
									<time 
										datetime={featuredPost.data.pubDate.toISOString()}
										class="text-text-primary uppercase tracking-wide font-bold"
									>
										<FormattedDate date={featuredPost.data.pubDate} />
									</time>
								</div>
							</div>
							
							<!-- Specimen content -->
							<div class="p-8 md:p-12">
								<h3 class="font-heading text-2xl md:text-4xl font-bold leading-[1.3] mb-6 group-hover:text-accent transition-colors duration-normal tracking-tight">
									{featuredPost.data.title}
								</h3>
								<p class="text-base md:text-lg leading-[1.8] text-text-secondary border-l-2 border-border pl-6">
									{featuredPost.data.description}
								</p>
							</div>
							
							<!-- Specimen footer -->
							<div class="border-t border-border px-8 py-4 font-mono text-xs text-text-muted bg-surface-alt">
								<div class="flex items-center gap-6">
									<span>STATUS: Published</span>
									<span>TYPE: Essay</span>
									<span class="text-accent">→ READ_ENTRY</span>
								</div>
							</div>
						</a>
					</article>
				</section>
			)}

			<!-- Archive Catalog -->
			{olderPosts.length > 0 && (
				<section class="max-w-6xl mx-auto mb-16">
					<div class="border-t border-border mb-6"></div>
					<div class="font-mono text-xs uppercase tracking-wider text-text-muted mb-6">
						ARCHIVE // EARLIER SPECIMENS
					</div>
					
					<ul class="border-2 border-border divide-y divide-border bg-surface">
						{olderPosts.map((post, index) => (
							<li class="group hover:bg-accent-subtle transition-colors duration-normal">
								<a 
									href={`/blog/${post.id}/`}
									class="flex flex-col md:flex-row md:items-center gap-3 px-6 py-5"
								>
									<div class="font-mono text-xs text-text-muted md:w-20 tracking-wider">
										{String(index + 2).padStart(3, '0')}
									</div>
									<h3 class="font-heading text-base md:text-lg leading-[1.3] group-hover:text-accent transition-colors flex-1 tracking-tight">
										{post.data.title}
									</h3>
									<time 
										datetime={post.data.pubDate.toISOString()}
										class="font-mono text-xs text-text-muted md:w-28 uppercase tracking-wide"
									>
										<FormattedDate date={post.data.pubDate} />
									</time>
									<div class="font-mono text-xs text-accent opacity-0 group-hover:opacity-100 transition-opacity md:w-8 text-right">
										→
									</div>
								</a>
							</li>
						))}
					</ul>
				</section>
			)}
		</main>
		
		<Footer />
	</body>
</html>
